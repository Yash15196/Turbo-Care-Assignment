package com.example.turbocaretestassignment.rhs.app.base.common

/**
 * @author yash gupta
 * A listener to be used by fragments to handle dialog button click events
 */
interface OnDialogButtonClickListener {
    fun onClick()
}